class watthetime:
    def __init__(self, t):
        #print("call to watthetime class init, arg = %s" % t)
        self.ttime = t
    
    def say_it(self):
        #print("Time is: %s" % self.ttime)
        return self.ttime
